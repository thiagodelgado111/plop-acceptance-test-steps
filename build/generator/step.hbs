
module.exports = function () {
  this.{{titleCase type}}(/^{{{parseScenarioText stepName type}}}$/, function ({{parseArgsNumber stepName}}) {
    // TODO: Write the step definition code here
    pending();
  });
};
