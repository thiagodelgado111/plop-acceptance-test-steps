
module.exports = function () {
  this.{{titleCase type}}(/^{{{parseScenarioText stepName type}}}$/, function ({{parseArgsNumber stepName addDoneCallback}}) {
    // @TODO: Write the step definition code here
    pending();
  });
};
